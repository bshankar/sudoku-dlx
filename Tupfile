WARNINGS += -Wall -Wextra
CFLAGS = $(WARNINGS) -O2 -march=native
LDFLAGS = -flto

: foreach src/*.cpp src/solvers/*.cpp src/solvers/Dlx/*.cpp src/problems/*.cpp src/problems/Sudoku/*.cpp |> g++ -c %f -o %o $(CFLAGS) |> build/obj/%B.o
: build/obj/*.o |> g++ %f -o %o $(LDFLAGS) |> build/Sudoku
